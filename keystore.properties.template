# Copy this file to `keystore.properties` (same directory) and fill in real values.
# `keystore.properties` is gitignored — never commit it or the .jks file.
# storeFile is resolved relative to the project root.
storeFile=arduinobt-upload-key.jks
storePassword=CHANGE_ME
keyAlias=arduinobt-upload
keyPassword=CHANGE_ME
